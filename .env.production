# Baked into production builds when Render/CI does not override.
# REST + WebSocket base (no trailing slash).
#
# Railway backend (crispro-backend-v2-production.up.railway.app) is
# currently unreachable (project b8b59448-403c-4b89-96f2-eede807ac867
# — user to confirm live URL). Falling back to the Render backend
# until Railway is confirmed. Both auto-deploy from the same main branch.
VITE_API_ROOT=https://crispro-backend-v2.onrender.com
VITE_WS_ROOT=https://crispro-backend-v2.onrender.com
